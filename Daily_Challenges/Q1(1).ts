function ascendingSequence(arr:number[]):void
{
    let curLen=1;
    let maxLen=1;
    let startIndex=0;
    let maxStartIndex=0;
    for(let i=1;i<arr.length;i++){
        if(arr[i]>arr[i]-1){
            curLen++;
        }else{
            if(curLen>maxLen)
                maxLen=cur
        }
    }


}